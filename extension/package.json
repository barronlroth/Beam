{
  "name": "@beam-lite/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "cp manifest.json dist/manifest.json",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.258",
    "jsdom": "^23.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "vitest": "^1.6.0"
  }
}
